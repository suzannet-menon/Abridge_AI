# Copy this file to `.env` and fill in your key.
# The app runs without it — research, architecture, builder, and brief fall
# back to the bundled deterministic agents when GEMINI_API_KEY is missing.

GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-3.6-flash
PORT=3001